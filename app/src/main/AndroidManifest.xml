<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Asal Hotel"
        android:theme="@style/Theme.Hotel"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
       >
        <activity
            android:name=".PreviewRoomInf"
            android:exported="true" />
        <activity
            android:name=".Roominfo"
            android:exported="true" />
        <activity
            android:name=".customerinfo"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".roomsPage" />
        <activity android:name=".roomDetails" />
        <activity android:name=".sameRoomType" />
    </application>

</manifest>